export type Info = {
  memberId: number;
  username: string;
  email: string;
  avatarUrl: string;
  accountNo: string;
  balance: number;
  verified: boolean;
};

export type PhoneNumber = {
  pheonNumber: string;
};
